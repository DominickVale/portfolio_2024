---
// import type { HTMLAttributes, Polymorphic } from 'astro/types'

// type Ap = {
//   href: string
// } & Polymorphic<{as: "a"}>

// type Bp = {
//   href: undefined
// } & Polymorphic<{as: "button"}>

// type Props = {
//   class?: string
//   small?: boolean
// } & ( Ap | Bp)

const { small, class: classes, ...rest } = Astro.props

const Component = Astro.props.href ? "a" : "button"
---

<Component class:list={[ "text-xs font-black tracking-[0.2em] text-white", classes]} {...rest}>
  <slot />
</Component>
