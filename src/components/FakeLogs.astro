<pre
  class="fake-logs pointer-events-none ml-4 mt-[8vh] hidden min-h-[7.2rem] text-xxs uppercase opacity-50 xl:block">
    {
      `
{
    const projects = await api.getProjects()
    logger.info("DONE")
    for (const p of projects) {
      this.renderData(p)
    }
    // . . .
    logger.info("DONE").
}      `}
</pre>

<style>
  @media screen and (max-height: 640px) {
    pre {
      display: none;
    }
  }
</style>

<script>
  import Typewriter from '../app/modules/animations/Typewriter'
  import { $all } from '../app/utils'

  const fakeLogs = $all('.fake-logs')
  fakeLogs.forEach((el) => {
    Typewriter.typewrite(
      el,
      `
{
    // loading proj_id: 001
    // done.
    // rendering data
    // . . .
    // add obj(“img_003.jpg”)
    // done.
}
`,
      2,
      2,
    )
  })
</script>
